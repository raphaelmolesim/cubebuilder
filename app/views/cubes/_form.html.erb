<%= form_for(cube) do |f| %>
  <% if cube.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cube.errors.count, "error") %> prohibited this cube from being saved:</h2>

      <ul>
      <% cube.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :password %>
    <%= f.text_field :password %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
